[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd backend && npm install", "cd frontend && npm install"]

[phases.build]
cmds = ["cd backend && npm run build", "cd frontend && VITE_API_BASE_URL=/api npx vite build"]

[start]
cmd = "cd backend && npm start"
