[phases.setup]
nixPkgs = ["python311", "nodejs"]

[phases.install]
cmds = ["cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && VITE_API_BASE_URL=/api npx vite build"]

[start]
cmd = "python server.py"
